version=0.24

source=(http://downloads.tuxfamily.org/nutyx/sources/exiv2/$name-$version.tar.gz)

build() {
cd $name-$version
	./configure --prefix=/usr \
		 --disable-static 
	make
	make DESTDIR=$PKG install
}

  
