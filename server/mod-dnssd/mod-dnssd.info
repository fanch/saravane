Apache HTTPD module which adds Zeroconf support via DNS-SD using Avahi.
This allows Apache to advertise itself and the websites available 
for customers with compatible protocol.
Module Apache HTTPD qui ajoute le support Zeroconf via DNS-SD en utilisant Avahi.
Cela autorise Apache à annoncer lui-même et les sites disponibles
pour les clients compatibles avec le protocole. 

http://0pointer.de/lennart/projects/mod_dnssd

Packagers:

pierre at nutyx dot org
