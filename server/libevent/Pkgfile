version=2.0.21

source=( https://github.com/downloads/$name/$name/$name-$version-stable.tar.gz)
build() {
cd $name-$version-stable
./configure --prefix=/usr --disable-static
make
make DESTDIR=$PKG install
}
