version=1.0
source=(http://downloads.sourceforge.net/freeassociation/$name/$name-$version.tar.gz)

build() {
cd $name-$version
mkdir build
cd build
cmake -DCMAKE_INSTALL_PREFIX=/usr \
      -DCMAKE_BUILD_TYPE=Release  \
      ..
make
make DESTDIR=$PKG install
}
