# Description: Client library used to access and manage the Zeitgeist event log from languages such as C and Vala.
# URL: http://www.gnome.org/
# Packager: pierre at nutyx dot org
# Depends on: glib 
version=0.3.18
source=(http://launchpad.net/$name/${version%.*}/$version/+download/$name-$version.tar.gz)

build() {
cd $name-$version
./configure --prefix=/usr \
--disable-static	
make
make DESTDIR=$PKG install
}
