# Description: lftp is a sophisticated ftp/http client.
# URL: lftp is a sophisticated ftp/http client.
lftp est un client ftp/http sophistiqué.
http://lftp.yar.ru/
# Packager: alienus at nutyx dot org
# Depends on: 
name=lftp
version=4.6.1
release=1

source=(http://lftp.yar.ru/ftp/$name-$version.tar.xz)
build() {
cd $name-$version
./configure --prefix=/usr \
--with-openssl \
--disable-static
make
make DESTDIR=$PKG install
}
