version=0.11.3d
_name=kmplayer
source=(http://kmplayer.kde.org/pkgs/${_name}-0.11.3d.tar.bz2)
build() {
cd ${_name}-$version
mkdir build
cd build
cmake -DCMAKE_INSTALL_PREFIX=$KDE_PREFIX \
-DCMAKE_BUILD_TYPE=release \
..
make
make DESTDIR=$PKG install
}
