version=5.1.1
_name=libmm-qt
source=(http://download.kde.org/stable/plasma/5.1.1/${_name}-$version.tar.xz )

build() {
cd ${_name}*
mkdir build
cd    build
cmake -DCMAKE_INSTALL_PREFIX=/usr \
      -DCMAKE_BUILD_TYPE=Release         \
      -DLIB_INSTALL_DIR=lib              \
      -DBUILD_TESTING=OFF                \
      -DECM_MKSPECS_INSTALL_DIR=/usr/share/qt5/mkspecs/modules \
      -Wno-dev .. 
make
make DESTDIR=$PKG install
}
