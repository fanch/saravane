# Description: An image viewing and cataloging program for MATE (GTK2 version)
# URL: http://matsusoft.com.ar/projects/mate
# Maintainer: NuTyX core team
# Packager: fanchyannmaria at orange dot fr
# Depends on: dbus-glib,desktop-file-utils,gtk,exempi,lcms2,libexif,libjpeg-turbo,mate-desktop,mate-icon-theme,python-gtk,python,python-gobject2,startup-notification,zlib

name=eom
version=1.8.1
release=1
source=(http://pub.mate-desktop.org/releases/${version%.*}/$name-$version.tar.xz)

build() {
cd $name-$version
 ./configure \
        --prefix=/usr \
        --localstatedir=/var \
        --with-gtk=2.0 \
        --with-librsvg
make
make DESTDIR=$PKG install
}
