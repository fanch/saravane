version=0.5.8
PKGMK_IGNORE_UNPACK="yes"
source=(http://www.midori-browser.org/downloads/midori_${version}_all_.tar.bz2)
build () {
mkdir $name-$version
tar xf midori_${version}_all_.tar.bz2 -C midori-$version
cd $name-$version
./configure --prefix=/usr
make
make DESTDIR=$PKG install
}
