version=1.5

source=( ftp://ftp.kde.org/pub/kde/stable/konversation/$version/src/$name-$version.tar.xz)

build() {
	cd $name-$version
	mkdir build
	cd build
	cmake -DCMAKE_INSTALL_PREFIX=$KDE_PREFIX \
      		-DSYSCONF_INSTALL_DIR=/etc/kde \
      		.. 
	make
	make DESTDIR=$PKG install
}

