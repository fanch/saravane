version=1.0.5
_name=makedepend

source=(http://downloads.nutyx.org/archives/xorg/individual/util/${_name}-$version.tar.bz2)

build() {
cd ${_name}-$version
./configure $XORG_CONFIG
make
make DESTDIR=$PKG install
}
