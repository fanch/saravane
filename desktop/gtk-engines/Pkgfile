version=2.20.2
source=(
http://downloads.nutyx.org/archives/gnome/sources/${name}/${version%.*}/${name}-$version.tar.bz2)

build() {
    cd ${name}-$version
    ./configure --prefix=/usr \
                --enable-animation \
                --disable-static
    make
    make DESTDIR=$PKG install
}

