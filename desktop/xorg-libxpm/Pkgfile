version=3.5.11

source=(http://mirror.switch.ch/ftp/mirror/X11/pub/individual/lib/libXpm-$version.tar.bz2)

build() {
cd libXpm-$version
./configure --prefix=/usr --sysconfdir=/etc --localstatedir=/var
make
make DESTDIR=$PKG install
}
