version=1.0.8

source=(http://downloads.nutyx.org/archives/xorg/individual/lib/libxkbfile-$version.tar.bz2)

build() {
	cd libxkbfile-$version

	./configure $XORG_CONFIG

	make
	make DESTDIR=$PKG install
}
