# Description:  Editeur de menu MATE
# URL: http://matsusoft.com.ar/projects/mate
# Maintainer: NuTyX core team
# Packager: fanchyannmaria at orange dot fr
# Depends on: caja,desktop-file-utils,enchant,gtk,gtksourceview,iso-codes,xorg-libsm,mate-desktop,python,python-gobject2,python-gtksourceview2
# Run on: mate-common,XML-parser,yelp-tools

name=pluma
version=1.8.1
release=1
source=(http://git.mate-desktop.org/$name/snapshot/$name-$version.tar.xz)

build() {
cd $name-$version
./autogen.sh \
        --prefix=/usr \
        --libexecdir=/usr/lib/${pkgname} \
        --with-gtk=2.0 \
        --enable-gtk-doc \
        --enable-python
make
make DESTDIR=$PKG install
}
