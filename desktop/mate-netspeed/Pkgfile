# Description: A MATE applet that shows how much traffic occurs on a specified network device.
# URL: http://matsusoft.com.ar/projects/mate
# Maintainer: NuTyX core team
# Packager: fanchyannmaria at orange dot fr
# Depends on: gtk libgtop mate-desktop mate-panel 

name=mate-netspeed
version=1.10.0
release=1
source=(http://pub.mate-desktop.org/releases/${version%.*}/$name-$version.tar.xz)

build() {
cd $name-$version
./configure \
        --prefix=/usr \
        --libexecdir=/usr/lib/${name} \
        --with-gtk=2.0 \
        --disable-static
make
make DESTDIR=$PKG install
}
