version=0.3.0

source=(http://downloads.nutyx.org/archives/xfce/src/apps/$name/${version%.*}/$name-$version.tar.bz2)

build() {
cd $name-$version
./configure --prefix=$XFCE4_PREFIX \
--sysconfdir=/etc \
--disable-static
make
make DESTDIR=$PKG install
}
