# Description: Applets for MATE panel
# URL: http://matsusoft.com.ar/projects/mate
# Maintainer: NuTyX core team
# Packager: fanchyannmaria at orange dot fr
# Depends on: gstreamer-plugins-base,libgtop, libwnck, system-tools-backends, mate-system-monitor, mate-settings-daemon, mate-icon-theme, networkmanager

name=mate-applets
version=1.8.1
release=1
source=(http://pub.mate-desktop.org/releases/${version%.*}/$name-$version.tar.xz)
build() {
cd $name-$version
 ./configure \
        --prefix=/usr \
        --sysconfdir=/etc \
        --libexecdir=/usr/lib/${name} \
        --enable-polkit \
        --enable-networkmanager \
        --enable-ipv6 \
        --disable-static
make
make DESTDIR=$PKG install
}
