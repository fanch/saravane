version=1.0.8

source=(http://mirror.switch.ch/ftp/mirror/X11/pub/individual/lib/libXvMC-$version.tar.bz2)

build() {
cd libXvMC-$version
./configure --prefix=/usr --sysconfdir=/etc --localstatedir=/var
make
make DESTDIR=$PKG install
}
