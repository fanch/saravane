version=1.0.4
_name=encodings

source=(http://downloads.nutyx.org/archives/xorg/individual/font/${_name}-$version.tar.bz2)

build() {
	cd ${_name}-$version

	./configure $XORG_CONFIG

	make
	make DESTDIR=$PKG install
}
