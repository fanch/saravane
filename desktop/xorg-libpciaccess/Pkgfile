version=0.13.2

_name=libpciaccess
source=(http://downloads.nutyx.org/archives/xorg/individual/lib/${_name}-$version.tar.bz2)

build() {
	cd ${_name}-$version

	./configure --prefix=/usr

	make
	make DESTDIR=$PKG install
}
