version=1.3.0
source=(ftp://ftp.kde.org/pub/kde/stable/frameworks/5.3.0/$name-$version.tar.xz)

build() {
cd $name-$version
mkdir build
cd    build

cmake -DCMAKE_INSTALL_PREFIX=/usr .. 
make
make DESTDIR=$PKG install
}
