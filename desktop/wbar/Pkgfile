version=2.3.4

source=(http://wbar.googlecode.com/files/$name-$version.tgz )

build() {
        cd $name-$version

        ./configure --prefix=/usr \
                --exec_prefix=/usr \
                --sysconfdir=/etc
        make
        make DESTDIR=$PKG install
}

