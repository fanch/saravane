version=0.6.3

source=( https://red.libssh.org/attachments/download/87/$name-$version.tar.xz)
	
build() {
	mkdir build
  	cd build
	cmake ../$name-$version \
    		-DCMAKE_INSTALL_PREFIX=/usr \
    		-DCMAKE_BUILD_TYPE=Release
	make
	make DESTDIR=$PKG install
}

